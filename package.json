{
  "name": "consumer",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@adiwajshing/baileys": "4.4.0",
    "fastify": "^4.12.0",
    "qrcode-terminal": "^0.12.0",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "dev": "npx ts-node-dev --respawn --transpile-only ./src/start/httpserver.ts",
    "start": "nodemon --watch src/start/httpserver -e ts --exec \"(tsc && node dist/index.js) || exit 1\""
  }
}
